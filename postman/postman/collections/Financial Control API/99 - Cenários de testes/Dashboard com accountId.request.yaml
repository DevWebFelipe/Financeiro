$kind: http-request
description: accountId não existe em GET /dashboard (visão consolidada; D285). Parâmetro desconhecido → 400 VALIDATION_ERROR.
url: "{{baseUrl}}/api/v1/dashboard?accountId={{accountId}}"
method: GET
queryParams:
  - key: accountId
    value: "{{accountId}}"
scripts:
  - type: afterResponse
    code: "const json = pm.response.json();\r\n\r\npm.test(\"Status deve ser 400 Bad Request\", function () {\r\n    pm.response.to.have.status(400);\r\n});\r\n\r\npm.test(\"Código de erro deve ser VALIDATION_ERROR\", function () {\r\n    pm.expect(json.code).to.eql(\"VALIDATION_ERROR\");\r\n});\r"
    language: text/javascript
order: 11400
